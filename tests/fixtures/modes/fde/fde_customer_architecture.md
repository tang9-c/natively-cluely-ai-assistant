# Customer Architecture

Customer architecture uses Salesforce, Snowflake, and Okta SSO.

The operations data warehouse is Snowflake. Identity is controlled through Okta SSO with SAML. Salesforce stores account-level workflow status.
