import React from 'react';

interface ProfileVisualizerProps {
    profileData: unknown;
}

/**
 * Local profile visualizer placeholder.
 *
 * The previous runtime path could be replaced by an optional external module.
 * Keep a stable local component here so Profile Intelligence never depends on
 * that directory existing.
 */
export const ProfileVisualizer: React.FC<ProfileVisualizerProps> = () => null;

export default ProfileVisualizer;
